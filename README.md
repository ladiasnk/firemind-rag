# firemind-rag
Ask natural language questions over a wildfire knowledge base. It ingests documents → chunks them → embeds them → stores in a local vector DB → retrieves relevant chunks → generates a grounded answer via an LLM.
